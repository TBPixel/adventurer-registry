# Adventurer Registry - Discord Bot

A discord bot for registering a discord message as a "character" under a given name, and allowing users to retrieve those characters by name, all within a server.

